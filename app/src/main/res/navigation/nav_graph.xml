<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/profileFragment">
    <fragment
        android:id="@+id/profileFragment"
        android:name="com.mkandeel.gallery.ui.ProfileFragment"
        android:label="fragment_profile"
        tools:layout="@layout/fragment_profile">

    </fragment>
    <fragment
        android:id="@+id/photosFragment"
        android:name="com.mkandeel.gallery.ui.PhotosFragment"
        android:label="fragment_photos"
        tools:layout="@layout/fragment_photos">
    </fragment>
    <fragment
        android:id="@+id/viewImageFragment"
        android:name="com.mkandeel.gallery.ui.ViewImageFragment"
        android:label="fragment_view_image"
        tools:layout="@layout/fragment_view_image" />

</navigation>